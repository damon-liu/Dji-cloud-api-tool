# DJI Cloud API 监控客户端 — 用户使用指南

## 一、这是什么？

本工具通过 MQTT 协议连接 DJI Cloud API，实时接收并展示机场和无人机的Topic数据，主要用于日常对接调试大疆上云API（第三方机场如果提供类似大疆上云API也可使用），是大疆行业设备开发者和运维人员的必备调试工具。

### 1.1 核心功能

- **多环境一键切换**：支持多个 Connection 配置，生产/测试环境互不干扰
- **设备树形管理**：机场 + 无人机层级展示，在线状态实时指示，支持新增/删除/重命名
- **机场飞机自动订阅**：连接机场后自动识别并添加关联的无人机
- **设备 OSD 面板**：机场信息与飞机信息并排显示，刷新间隔可调，鼠标悬停显示字段 key、点击可复制
- **Topic 订阅管理**：每个设备独立管理 Topic，自动订阅大疆机场 7 个常用 Topic，支持新增/删除/启用/禁用/拖拽排序
- **上报JSON管理**：按设备 Topic 过滤报文，支持暂停滚动、一键复制、清除历史
- **上报JSON抓包导出**：一键抓包，数据实时写入 `captures/` 文件夹，停止后弹窗显示路径和记录条数
- **上报JSON 字段自动翻译**：根据大疆上云官网自动翻译 Topic 上报 json 参数 key，按分组展示，支持网格和列表分组查看
- **Topic 下发**：自动预设常用下发 Topic，每个 Topic 提供官方下发参数 JSON 模板
- **Topic 下发记录**：自动记录每次下发与响应，成功/失败一目了然
- **控制中心-机场控制**：集成远程调试、飞机电源、机场舱盖、飞机充电、机场维护、强制关舱门等快捷控制，实时显示发送及响应消息记录
- **控制中心-飞行控制**：支持飞行控制权限抢夺/释放、一键起飞/返航/取消返航/紧急停机等飞行指令；
- **控制中心-负载控制**：支持负载控制权限抢夺/释放、拍照/录像/云台回中等负载控制；支持多机场下拉切换
- **控制中心-运维模式**：支持机场重启、强制关舱门、补光灯等运维操作
- **视频直播**：飞机/机场双窗口独立直播，支持镜头切换（红外/变焦/广角）、视频源切换（内外视频）、5档清晰度调节【下版本实现】
- **自动重连**：指数退避（1s → 2s → … → 最长 30s），恢复后自动刷新
- **断线保护**：断线自动暂停面板刷新，手动暂停优先级更高，不打断数据查看

---

### 1.2 对比 MQTTX 优点

很多开发者使用 [MQTTX](https://mqttx.app/) 进行 MQTT 调试，但针对 DJI Cloud API 场景，本工具有以下明显优势：

| 对比维度 | MQTTX | DjiCloudApiTool |
|----------|-------|-----------------|
| 设备识别 | 只能看到 Topic 字符串，需人工判断是哪个设备 | 自动识别设备 SN，树形展示机场与子飞机的层级关系 |
| 设备管理 | 所有 Topic 混在一起，无层级结构 | 每个设备独立管理 Topic，支持启用/禁用/拖拽排序 |
| 自动关联子设备 | 无 | 机场添加以后发现并订阅子飞机 |
| 自动订阅 | 无（需手动逐个添加 Topic） | 添加机场自动订阅 7 个Topic |
| 上报JSON管理 | 无分类筛选能力 | 支持原始 JSON 实时滚动、暂停/恢复、一键复制、清除历史 |
| JSON抓包导出 | 需手动复制粘贴或配置外部工具 | 一键抓包，数据实时写入 `captures/` 文件夹，按设备/Topic 分类保存 |
| JSON自动翻译 | 原始 JSON，字段名全英文（如 `drone_in_dock`） | 自动翻译为中文，按分组展示，一目了然 |
| Topic 指令下发 | 手动输入 Topic 字符串 + 手写 JSON 参数 | 自动预设常用6个下发 Topic，每个Topic提供官方下发参数 JSON模板 |
| Topic下发记录 | 需自行保存或外部记录 | 自动记录每次下发与响应，成功/失败一目了然 |
| 机场控制 | 需手动拼装远程调试/电源/舱盖/充电等指令 JSON | 点击按钮一键控制，进入远程调试后可用，实时反馈执行结果 |
| 飞行控制 | 需手动拼装起飞/返航/降落指令 JSON | 独立飞行控制窗口，一键起飞/返航/降落，含飞行器状态反馈 |
| 负载控制 | 需手动拼装拍照/录像指令 JSON | 独立飞行控制窗口，一键拍照/录像 |
| 运维模式 | 需手动拼装运维功能等指令 JSON | 独立运维窗口，集成日常运维所需的功能按钮 |
| 视频直播 | 无视频直播能力 | 飞机/机场双窗口独立直播，支持飞机多镜头、机场内外视频源切换、清晰度调节 |
| 环境依赖 | 需安装 Node.js 或下载桌面版 | 可执行文件 exe，无需任何依赖 |
| 学习成本 | 通用 MQTT 工具，需自行理解 DJI 协议 | 专为 DJI Cloud API 设计，开箱即用 |
| 行业定制 | 无                                            | 专门针对大疆上云定制版本，后续持续更新内容 |

> 💡 总结：MQTTX 是优秀的通用 MQTT 调试利器，但 DjiCloudApiTool 在 DJI 行业设备场景下效率更高——**设备层级树展示、自动关联订阅机场无人机、默认集成设备上下行的所有 Topic、Topic 抓包导出、JSON 自动翻译、Topic指令下发及下发记录、机场快捷控制、飞行控制、负载控制、视频直播**等功能都是针对大疆设备的专属优化。

---

## 二、下载与安装

去 GitHub 下载最新版本，下载 `DjiCloudApiTool-v1.0.3.zip`，解压后双击运行 `DjiCloudApi.exe`，**不需要安装**。

👉 https://github.com/damon-liu/Dji-cloud-api-tool/releases/tag/v1.0.3

![image-20260710155056204](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710155056204.png)

---

## 三、操作说明

### 3.1 连接 MQTT 服务器

打开软件后，先连上你的 MQTT Broker（这是数据来源），按如下操作：

**①** 点击左上角「⚙ 配置」按钮会弹出一个配置窗口：

![image-20260710170652328](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710170652328.png)

**②** 填写Broker参数

| 参数 | 说明 | 示例 |
|------|------|------|
| Broker IP | MQTT 服务器地址 | 127.0.0.1 |
| 端口 | 一般是 8883 | 8883 |
| 用户名 | MQTT 用户名 | admin |
| 密码 | MQTT 密码 | 你的密码 |

**③** 点「Test」测试连接

> ⚠️ 如果提示"连接失败请检查配置参数是否有误"，请确认 IP、端口、用户名、密码无误。



**④** 测试通过后，点「OK」保存

---

### 3.2、添加设备

连上服务器后，需添加设备。

![image-20260710161318247](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710161318247.png)

**①** 点击左侧「＋」按钮

**②** 选择设备类型

> **Dock (机场)**：大疆机场/机库设备
> **Pilot (手飞飞机)**：无人机

**③** 输入设备 SN 和设备名称

**④** 点击确定，左侧会显示设备列表：

🟢 绿色 = 设备在线
🔴 红色 = 设备离线

> 💡 **自动发现子飞机**：连接机场后，软件会自动识别该机场下挂载的无人机并自动添加到设备列表，无需手动添加。

### 3.3 设备列表操作

| 操作 | 方式 |
|------|------|
| 选中设备 | 单击设备行 |
| 复制设备 SN | 双击设备行 |
| 重命名设备 | 右键设备 → 重命名 |
| 添加子设备 | 选中机场 → 点「＋」→ 为机场添加手飞无人机 |
| 删除设备 | 选中设备 → 点「✕」 |

---

### 3.4  连接监控

点右上角 **「● 连接」** 按钮，软件开始接收数据，连接成功后：

> 1、状态栏显示「🟢 已连接」
> 2、右侧面板开始实时刷新数据
> 3、原始 JSON 窗口开始滚动显示报文
> 4、自动发现机场下的无人机并添加订阅

---

## 四、界面功能详解

![image-20260710165818685](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710165818685.png)

### 📡 OSD 实时面板

默认 **1 秒刷新一次**，可手动调整。


机场信息（选中 Dock 设备时显示）

| 字段     | 含义             |
| -------- | ---------------- |
| 经纬度   | 机场 GPS 坐标    |
| 舱盖     | 打开 or 关闭     |
| 飞行器   | 在舱内 or 已离舱 |
| 舱内温度 | 机库内部温度     |
| 环境温度 | 室外温度         |
| 风速     | 当前风速         |
| 降雨量   | 当前降雨量       |

飞机信息（与机场并排显示）

| 字段     | 含义                                 |
| -------- | ------------------------------------ |
| 飞行状态 | 待机 / 起飞 / 航线飞行 / 返航 / 降落 |
| 经纬度   | 飞机当前 GPS 坐标                    |
| 电量     | 电池剩余百分比                       |
| 电池温度 | 电池温度                             |
| 高度     | 相对起飞点高度                       |
| 距Home   | 距起飞点距离                         |
| 风速     | 当前风速                             |
| GPS搜星  | GPS 卫星数                           |

---

### 🔍 JSON 解析

把原始 JSON 数据翻译成中文，方便阅读。

📌 **点击左侧 Topic 切换**，显示 Topic 的解析数据
⏸ 支持暂停 / 继续
⏱ 刷新间隔可调（默认 2 秒）
📋 **点击任意字段名** → 复制 JSON key 到剪贴板
📦 数据按分组展示

---

### 🗂 原始 JSON 面板

显示 MQTT 原始报文，适合技术人员查看，每条数据包含：

```
[topic] thing/product/dock001/osd
{"tid":"...","data":{...}}
[time] 2026-07-10 14:30:25
```


操作按钮：

| 按钮   | 功能                             |
| ------ | -------------------------------- |
| ⬤ 抓包 | 开始/停止导出数据到文件          |
| ⏸ 暂停 | 暂停界面显示，但数据仍在后台接收 |
| 📋 复制 | 复制当前窗口全部 JSON 内容       |
| 🗑 清除 | 清空当前显示的历史记录           |

---

### 📦 抓包导出

把接收到的数据保存到文件，方便事后分析。


![image-20260710161154072](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710161154072.png)

操作步骤：

1、点击 **「⬤ 抓包」** 按钮
2、数据实时写入 `captures/` 文件夹
3、点击 **「⏹ 停止」** 结束抓包
4、弹窗显示文件路径和记录条数
5、点击 **「打开文件夹」** 直接定位到文件

文件命名规则：`设备SN-topic类型-时间.json`，文件格式：

```json
[
    {"tid":"...","data":{...}},
    {"tid":"...","data":{...}}
]
```

---

### 📋 Topic 管理

![image-20260710170047954](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710170047954.png)

选中设备后，下方显示该设备订阅的所有 Topic：

自动添加 7 个常用 Topic（默认禁用状态），需要的时候手动启用即可。包括 state、requests、events、services_reply、property/set_reply、status、drc/up。

> 💡 **拖拽调整高度**：设备树与 Topic 列表之间的分隔线支持上下拖拽，方便在设备多或 Topic 多时灵活调整可视区域。

| 按钮 | 功能 |
|------|------|
| ＋ | 新增自定义 Topic |
| ◎ | 启用/禁用当前选中的 Topic |
| ✕ | 删除选中的 Topic |
| ▲▼ | 上下移动调整 Topic 顺序 |
| ⊘ | 一键全部启用 / 全部禁用（智能切换） |

---

### 🔽 Topic 下发面板

界面底部有一个折叠面板「▶ Topic 下发」，点击可展开，支持向设备发送 MQTT 消息。

![image-20260710170047954](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710170047954.png)

**已集成 6 个常用下发 Topic：**

| Topic | 用途 |
|-------|------|
| `property/set` | 设备属性设置 |
| `services` | 服务调用 |
| `events_reply` | 事件回复 |
| `requests_reply` | 请求回复 |
| `drc/down` | DRC 下行指令 |
| `status_reply` | 状态回复 |

**操作步骤：**

1. 在下拉框中选择要下发的 Topic → 自动填入参数模板
2. 在 JSON 编辑区修改参数值
3. 点击 **「发送」** 按钮下发消息
4. 发送记录自动保存到下方历史列表

**发送历史：**

| 操作 | 方式 |
|------|------|
| 查看历史 | 历史列表显示最近发送记录（Topic + 参数） |
| 恢复参数 | 双击历史记录 → JSON 编辑区自动恢复当时的参数 |
| 清空历史 | 点击「清除历史」按钮 |

> ⚠️ 下发功能需配合设备端正确的 Topic 权限，请确认设备已订阅对应的回复 Topic。

---

### ❓ 帮助按钮

工具栏右侧的 **「? 帮助」** 按钮提供快捷入口：

| 菜单项 | 跳转目标 |
|--------|----------|
| 大疆上云 API 官网 | DJI Cloud API 官方文档 |
| 项目地址 | GitHub 仓库主页 |

适合快速查阅 DJI 协议文档或反馈问题。

---

### 🔌 多连接配置

![image-20260710170121729](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710170121729.png)

**一套软件，多套环境切换。**

场景举例：
一个「生产环境」配置 → 连正式服务器
一个「测试环境」配置 → 连测试服务器

在配置窗口的 Connections 行：

| 按钮 | 功能 |
|------|------|
| ▼ | 下拉切换 Connection |
| ＋ | 新增一个 Connection |
| ✎ | 重命名当前 Connection |
| ✕ | 删除当前 Connection |

每个 Connection 有**独立的 MQTT 参数和设备列表**，互不干扰，切换 Connection 时，设备列表和所有数据会自动清空并重新加载。

## 五、实用小技巧

### 🖱 快捷操作汇总

| 操作 | 方式 |
|------|------|
| 复制设备 SN | 双击设备列表中的设备 |
| 复制 Topic | 双击 Topic 列表中的 Topic |
| 复制 JSON Key | 点击解析面板中的字段名 |
| 重命名设备 | 右键设备 → 重命名 |

### 🔄 断线自动重连

MQTT 意外断开时：

```
1、软件会自动尝试重连（首次等待 1 秒，逐次翻倍，最长 30 秒）
2、OSD 面板和 JSON 解析面板自动暂停刷新
3、重连成功后自动恢复，无需手动操作
```

---

## 六、常见问题

### 🖥 运行相关

**Q：下载 zip 包后双击 exe 没有运行按钮？**

![image-20260710155248613](C:\Users\lhx\AppData\Roaming\Typora\typora-user-images\image-20260710155248613.png)

> A：这是 Windows Defender 对陌生文件的正常拦截。点击「更多信息」→「仍要运行」即可。如果希望一劳永逸，将 exe 所在文件夹加入 Defender 排除列表。

---

### 📡 连接相关

**Q：连接测试失败？**

> A：提示"连接失败请检查配置参数是否有误"。请依次确认：
> Broker IP 和端口号是否正确
> 用户名和密码是否与 MQTT 服务器一致
> 电脑是否能 ping 通 MQTT 服务器（`ping 服务器IP`）

**Q：数据不显示？**

> A：请检查三项：
> MQTT 是否已连接（看底部状态栏是否为「🟢 已连接」）
> 设备 SN 是否正确（必须与实际设备序列号完全一致）
> Topic 是否已启用（列表中显示为 ● 实心，而非 ○ 空心）

---

### 📁 文件相关

**Q：抓包文件在哪？**

> A：保存在软件同目录下的 `captures/` 文件夹中。抓包停止后弹窗中有「打开文件夹」按钮可直接跳转。文件命名规则：`设备SN-topic类型-时间.json`。

**Q：配置文件保存在哪？**

> A：`config.json`，在软件同目录下，首次运行自动生成。

> ⚠️ 此文件包含 MQTT 连接密码，请勿分享给他人或提交到公开仓库。

---

### 🔧 使用相关

**Q：怎么同时监控多个设备？**

> A：点「＋」重复添加即可。Dock（机场）设备下还可以添加多架子飞机。

**Q：软件需要联网吗？**

> A：软件通过 MQTT 协议连接 DJI Cloud API Broker 获取实时数据，请确保电脑与 Broker 之间的网络畅通。

## 七、技术栈

| 层 | 技术 |
|----|------|
| 框架 | Qt 6 (Core, Widgets, Mqtt) |
| 语言 | C++ 17 |
| 构建 | CMake + MinGW |
| 协议 | MQTT 3.1.1 / 5.0 |
| 数据格式 | DJI Cloud API (JSON) |

---

## 八、下版本预告 (v1.1.0)

以下功能已在开发中，预计下个版本推出：

### 📺 视频直播功能实现

当前版本已完成视频直播 UI 框架搭建（飞机/机场双窗口、镜头切换、清晰度调节），下版本将实现完整的直播流播放能力，接入 DJI 视频流 SDK。

### 🔔 关键事件桌面通知

设置告警规则（如电量低于 20%、飞机降落、进入返航状态等），触发时通过 Windows 系统通知弹窗提醒。不用一直盯着面板，飞机状态变化第一时间知道。

### 🎨 暗色模式支持

新增暗色主题，适合夜间调试场景，减轻长时间使用的视觉疲劳。
